[workspace]
resolver = "2"
members = ["backend", "agent"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"

[profile.release]
opt-level = 3
codegen-units = 1
panic = "abort"
strip = true
incremental = false
